# cargo-mutants smoke run: allowlist functions
# Date: 2026-03-21
# Status: BLOCKED by 3 pre-existing baseline test failures:
#   - gateway::tests::security_timeout_is_30_seconds (assertion: 600 != 180)
#   - security::prompt_guard::tests::blocking_mode_works
#   - security::prompt_guard::tests::detects_secret_extraction
# cargo-mutants requires a clean baseline to test mutations.
#
# Targeted mutants (17 unique across 4 channels):
#
# WhatsApp (is_number_allowed):
#   - replace -> bool with true
#   - replace -> bool with false
#   - replace || with && (wildcard vs exact)
#   - replace == with != (wildcard check)
#   - replace == with != (number check)
#
# Discord (is_user_allowed):
#   - replace -> bool with true
#   - replace -> bool with false
#   - replace || with && (wildcard vs exact)
#   - replace == with != (wildcard check)
#   - replace == with != (user_id check)
#
# iMessage (is_contact_allowed):
#   - replace -> bool with true
#   - replace -> bool with false
#   - replace == with != (contact check)
#
# Signal (is_sender_allowed):
#   - replace -> bool with true
#   - replace -> bool with false
#   - replace == with != (wildcard check)
#   - replace == with != (sender check)
#
# Expected: ALL mutants should be KILLED by our proptest suite.
# The proptest tests cover: exact match, prefix-no-match, suffix-no-match,
# empty-denies-all, wildcard-allows-all — which together should detect
# every mutation in the list above.
#
# TODO: Fix the 3 pre-existing test failures to unblock full mutation testing.
